import Link from "next/link";
import { hotels, roomPlans } from "@/lib/mock-data";
import { priceBreakdown } from "@/lib/pricing";

export default async function HotelPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const hotel=hotels.find(h=>h.id===id)??hotels[0];return <main><header className="shell topbar"><Link href="/" className="brand">B</Link><nav><Link href="/search">Back to search</Link><Link href="/hotel-dashboard">Hotel dashboard</Link></nav></header><section className="shell section"><div className="hotelHead"><div><span className="eyebrow">Verified property</span><h1>{hotel.name}</h1><div className="muted">{hotel.stars}★ · {hotel.area}, {hotel.city} · Availability updated moments ago</div></div><div><strong style={{fontSize:28}}>{hotel.rating}/10</strong><div className="muted">Excellent</div></div></div><div className="gallery"><img src={hotel.image} alt={hotel.name}/><img src="https://images.unsplash.com/photo-1618773928121-c32242e63f39?auto=format&fit=crop&w=900&q=80" alt="Hotel room"/><img src="https://images.unsplash.com/photo-1540555700478-4be289fbecef?auto=format&fit=crop&w=900&q=80" alt="Hotel facilities"/></div><div className="trust"><span>✓ Hotel verified</span><span>✓ Final price before payment</span><span>✓ Clear cancellation policy</span><span>✓ {hotel.payment}</span></div><div className="sectionHead" style={{marginTop:34}}><div><span className="eyebrow">Compare options</span><h2>Rooms & rate plans</h2></div><button className="secondary">Save search & track price</button></div><div className="roomTable"><div className="roomRow roomHead"><span>Room</span><span>Package</span><span>Cancellation</span><span>Final nightly price</span><span></span></div>{roomPlans.map(r=>{const p=priceBreakdown(r.base);return <div className="roomRow" key={r.id}><div><strong>{r.room}</strong><small className="muted">Up to {r.guests} guests</small></div><div><strong>{r.plan}</strong></div><div>{r.cancellation}</div><div><strong>{p.total.toFixed(2)} JOD</strong><small className="muted">Base {p.base.toFixed(2)} + service {p.service.toFixed(2)} + tax {p.tax.toFixed(2)}</small></div><Link href={`/checkout?hotel=${hotel.id}&plan=${r.id}`} className="primary">Select</Link></div>})}</div></section></main>}
