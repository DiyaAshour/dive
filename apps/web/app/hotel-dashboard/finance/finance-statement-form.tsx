"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

export function FinanceStatementForm({hotelId,locale}:{hotelId:string;locale:"en"|"ar"}){const router=useRouter();const today=new Date();const end=today.toISOString().slice(0,10);const start=new Date(Date.UTC(today.getUTCFullYear(),today.getUTCMonth(),1)).toISOString().slice(0,10);const [from,setFrom]=useState(start);const [to,setTo]=useState(end);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(event:React.FormEvent){event.preventDefault();setBusy(true);setError("");try{const response=await fetch(`/api/v1/hotels/${hotelId}/finance/statements`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({from,to})});const body=await response.json().catch(()=>null) as {error?:{message?:string}}|null;if(!response.ok)throw new Error(body?.error?.message??"Statement failed");router.refresh();}catch(error){setError(error instanceof Error?error.message:"Statement failed");}finally{setBusy(false)}}return <form className="financeStatementForm" onSubmit={submit}><label><span>{locale==="ar"?"من":"From"}</span><input type="date" value={from} onChange={(e)=>setFrom(e.target.value)} required/></label><label><span>{locale==="ar"?"إلى":"To"}</span><input type="date" value={to} onChange={(e)=>setTo(e.target.value)} required/></label><button disabled={busy} type="submit">{busy?(locale==="ar"?"جارٍ الإصدار…":"Issuing…"):(locale==="ar"?"إصدار كشف مالي":"Issue statement")}</button>{error&&<small className="opsError">{error}</small>}</form>}
