import { redirect } from "next/navigation";
import { Banknote, CalendarRange, ReceiptText, RotateCcw } from "lucide-react";
import { getHotelFinanceOverview, listUserHotels } from "@platform/server";
import { PartnerSidebar } from "@/components/partner-sidebar";
import { PartnerLanguageBar } from "@/components/partner-language-bar";
import { currentUser } from "@/lib/server-session";
import { requestLocale } from "@/lib/request-locale";
import { direction } from "@/lib/i18n";
import { FinanceStatementForm } from "./finance-statement-form";

export const dynamic="force-dynamic";

export default async function FinancePage({searchParams}:{searchParams:Promise<{hotelId?:string;days?:string}>}){const user=await currentUser();if(!user)redirect("/partner/login");const [locale,query]=await Promise.all([requestLocale(),searchParams]);const hotels=await listUserHotels(user.id);if(!hotels.length)redirect("/partner/onboarding");const selected=hotels.find((hotel)=>hotel.id===query.hotelId)??hotels[0];if(!selected)redirect("/hotel-dashboard");const days=Math.max(1,Math.min(Number(query.days??30)||30,366));const data=await getHotelFinanceOverview(user.id,selected.id,days);return <main className="partnerAppShell" dir={direction(locale)}><PartnerSidebar hotelId={selected.id} hotelName={data.hotel.name} active="finance" locale={locale}/><section className="partnerMain"><PartnerLanguageBar locale={locale}/><div className="partnerTopbar"><div><span className="partnerPageEyebrow">{locale==="ar"?"المالية والتسويات":"Finance & settlement"}</span><h1>{locale==="ar"?"المالية والفواتير":"Finance & statements"}</h1><p>{locale==="ar"?"الأرقام من سجل مالي append-only للحجوزات المؤكدة والتعديلات والإلغاءات.":"Figures come from the append-only booking financial ledger, including confirmed sales, modifications and cancellation adjustments."}</p></div><FinanceStatementForm hotelId={selected.id} locale={locale}/></div><div className="partnerKpiGrid financeKpis"><Metric icon={<Banknote size={17}/>} label={locale==="ar"?"إجمالي الحجوزات":"Booking gross"} value={money(data.totals.bookingGross,data.hotel.currency)}/><Metric icon={<ReceiptText size={17}/>} label={locale==="ar"?"عمولة المنصة":"Platform commission"} value={money(data.totals.platformCommission,data.hotel.currency)}/><Metric icon={<RotateCcw size={17}/>} label={locale==="ar"?"تعديلات الإلغاء":"Cancellation adjustments"} value={money(data.totals.cancellationAdjustments,data.hotel.currency)}/><Metric icon={<CalendarRange size={17}/>} label={locale==="ar"?"الفترة":"Reporting period"} value={`${data.period.days} ${locale==="ar"?"يوم":"days"}`}/></div><div className="financeGrid"><section className="partnerDataCard"><span className="partnerPageEyebrow">{locale==="ar"?"تفصيل السجل":"Ledger breakdown"}</span><h2>{locale==="ar"?"ماذا حدث مالياً؟":"What moved financially?"}</h2><div className="financeRows"><Row label={locale==="ar"?"أساس الغرفة":"Room base"} value={money(data.totals.roomBase,data.hotel.currency)}/><Row label={locale==="ar"?"رسوم الخدمة":"Service charge"} value={money(data.totals.serviceAmount,data.hotel.currency)}/><Row label={locale==="ar"?"الضريبة والرسوم":"Tax / charges"} value={money(data.totals.taxAmount,data.hotel.currency)}/><Row label={locale==="ar"?"المبالغ المستردة المسجلة":"Refund ledger"} value={money(data.totals.refunds,data.hotel.currency)}/><Row label={locale==="ar"?"عدد الأحداث المالية":"Financial events"} value={String(data.totals.eventCount)}/></div></section><section className="partnerDataCard"><span className="partnerPageEyebrow">{locale==="ar"?"الكشوف":"Statements"}</span><h2>{locale==="ar"?"كشوف مالية مؤرشفة":"Issued finance statements"}</h2>{data.statements.length===0?<div className="partnerEmpty">{locale==="ar"?"لم يتم إصدار كشف بعد.":"No statement has been issued yet."}</div>:<div className="statementList">{data.statements.map((statement)=><article key={statement.id}><div><strong>{statement.statementNumber}</strong><small>{statement.periodStart.toISOString().slice(0,10)} → {statement.periodEnd.toISOString().slice(0,10)}</small></div><div><b>{money(statement.bookingGross,statement.currency)}</b><span>{statement.status}</span></div></article>)}</div>}</section></div><section className="partnerDataCard"><span className="partnerPageEyebrow">{locale==="ar"?"الحجوزات":"Reservations"}</span><h2>{locale==="ar"?"أحدث الحركة التجارية":"Recent commercial activity"}</h2><div className="financeBookingList">{data.recentBookings.map((booking)=><article key={booking.id}><div><strong>{booking.reference}</strong><span>{booking.guestName}</span></div><span>{booking.status}</span><span>{booking.paymentMode} · {booking.paymentState}</span><b>{money(booking.totalAmount,booking.currency)}</b><small>{locale==="ar"?"عمولة":"Commission"} {money(booking.commissionAmount,booking.currency)}</small></article>)}</div></section></section></main>}
function Metric({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div><span className="financeMetricLabel">{icon}{label}</span><strong>{value}</strong></div>}
function Row({label,value}:{label:string;value:string}){return <div><span>{label}</span><strong>{value}</strong></div>}
function money(value:number,currency:string){return `${Number(value).toFixed(2)} ${currency}`}
